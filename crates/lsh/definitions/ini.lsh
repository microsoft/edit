#[display_name = "Properties"]
#[path = "**/*.ini"]
pub fn properties() {
    until /$/ {
        yield other;

        if /;.*/ {
            yield comment;
        } else if /'/ {
            single_quote_string();
        } else if /"/ {
            double_quote_string();
        }
    }
}
