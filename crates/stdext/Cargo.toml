[package]
name = "stdext"
version = "0.0.0"

edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[features]
single-threaded = []
superluminal = ["superluminal-perf-sys"]

[dependencies]
superluminal-perf-sys = { version = "0.4", optional = true }

[target.'cfg(unix)'.dependencies]
libc = "0.2"
